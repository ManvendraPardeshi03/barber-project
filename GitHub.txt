# 🧠 GITHUB – BARBER PROJECT (RECALL NOTES)

📌 **IMPORTANT**
    * All Git commands run from **project root only**
    * ❌ Never run git commands inside `backend/` or `frontend/`
    📁 Project Path: C:\Users\manve\Desktop\Barber Project

## 🔰 FIRST TIME SETUP (ONE TIME ONLY)
    git init
    git branch -M main
    git remote add origin https://github.com/ManvendraPardeshi03/barber-project.git

## 📦 DAILY WORKFLOW (MOST IMPORTANT)
    ### 1️⃣ CHECK CURRENT STATUS
        git status
            * Shows modified / untracked files
            * Always check before committing
    ### 2️⃣ STAGE CHANGES
        git add .
            * Adds **ALL backend + frontend changes**
    ### 3️⃣ COMMIT WITH CLEAR MESSAGE
        git commit -m "<what you fixed or added>"
            ✅ Examples:
            * `Fix login API baseURL for production`
            * `Fix slot picker and backend slot calculation`
            * `Enable CORS and update API URL`
    ### 4️⃣ PUSH TO GITHUB
        git push origin main
            * Uploads changes to GitHub
            * Triggers **Vercel / Render auto-deploy**
    ## 🔄 QUICK UPDATE FLOW (MEMORY LINE)
        git status
        git add .
        git commit -m "message"
        git push origin main

## ⚠️ COMMON SITUATIONS & FIXES
    ### 🔴 Nothing to commit
        nothing to commit, working tree clean
            ✅ Means everything is already pushed
    ### 🔴 File modified but not staged
        git add .
            Then commit again
    ### 🔴 Wrong file path error
        ❌ `git add service.routes.js`
        ✅ Correct way:
        git add backend/routes/service.routes.js

## 🧹 CLEANUP / SAFETY COMMANDS
    ### Undo local changes (⚠️ CAREFUL)
        git restore .
    ### Check commit history
        git log --oneline

## 🚀 DEPLOYMENT FLOW (REAL PROJECT)
    1. Make changes (Frontend / Backend)
    2. Test locally (Docker or npm)
    3. `git add .`
    4. `git commit -m "clear message"`
    5. `git push origin main`
    6. ✅ GitHub updated
    7. ✅ Vercel frontend redeploys
    8. ✅ Render backend redeploys
